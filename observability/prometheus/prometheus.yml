global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: deliverator-server
    metrics_path: /api/metrics
    static_configs:
      - targets:
          - server:3000
